#include "main.h"
#include <stdio.h>
/**
 * _strlen - Returns the lenght of a string.
 * @s: Type char pointer
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int y;

	for (y = 0; s[y] != 0; y++)
	{
	}
		return (y);

}
